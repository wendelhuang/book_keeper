<h1>Listing record_types</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @record_types.each do |record_type| %>
      <tr>
        <td><%= record_type.title %></td>
        <td><%= record_type.image %></td>
        <td><%= link_to 'Show', record_type %></td>
        <td><%= link_to 'Edit', edit_record_type_path(record_type) %></td>
        <td><%= link_to 'Destroy', record_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Record type', new_record_type_path %>
