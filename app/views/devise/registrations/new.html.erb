
<div class="panel panel-default">
  <%= render :partial => 'shared/panel_heading', :locals => {:title => '注册'} %>
  <div class="panel-body">
    <h1 class="text-center">注册</h1>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
                 :html => {:class => 'form-horizontal', :role => 'form'}) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :email, :class => "control-label col-sm-1 col-sm-offset-3" %>
        <div class="col-sm-5">
          <%= f.email_field :email, autofocus: true, :class => "form-control", :placeholder => "邮箱", :required => true %>
        </div>
      </div>
      
      <div class="form-group">
        <%= f.label :password, :class => "control-label col-sm-1 col-sm-offset-3" %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <div class="col-sm-5">
          <%= f.password_field :password, autocomplete: "off", :class => "form-control", :placeholder => "请输入密码", :required => true %>
        </div>
      </div>
      
      <div class="form-group">
        <%= f.label :password_confirmation, :class => "control-label col-sm-1 col-sm-offset-3" %>
        <div class="col-sm-5">
          <%= f.email_field :password_confirmation, autocomplete: "off", :class => "form-control", :placeholder => "请再次输入密码", :required => true %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-1 col-sm-offset-8">
          <%= f.submit :class => "btn btn-default" %>
        </div>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>

